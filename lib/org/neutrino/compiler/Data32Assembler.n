protocol Data32Assembler {

  @static def proto.new(name, binary) => new Data32Assembler {
    name := name,
    binary := binary,
    out := new BlobStream()
  }

  def this.data => this.out.get_blob();

  def this.write(arg is small_integer_t)
   => this.out.write_int32(arg.value << 2);

}
