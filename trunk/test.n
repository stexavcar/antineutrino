def main() -> "fnis":fooa(3, 4);

def fooa(x, y) -> foox(y);
def foox(x) -> fooy(x);
def fooy(x) -> fooz(x);
def fooz(x) -> fuzzing(this);

def fuzzing(v) {
  return (if (true) "flab".length());
}

def foob(x) -> 2;
def fooc(x, y) -> 1;
def food(x, y, z) -> 0;
def fooe(x, y, z, w) -> 8;

class True {

}

class String {
  def length() -> this;
}
