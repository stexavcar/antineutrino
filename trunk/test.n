def main() -> "fnis":fooa(3, 4);

def fooa(x, y) -> foox(y);
def foox(x) -> fooy(x);
def fooy(x) -> fooz(x);
def fooz(x) -> fuzzing(this, x);

def fuzzing(x, y) {
  return x.length() - 1 + y;
}

internal class SmallInteger {
  internal def operator +(that);
  internal def operator -(that);
}

internal class True {

}

internal class String {
  internal def length();
}
