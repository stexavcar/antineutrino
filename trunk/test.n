def main() -> fooa("knork", "knark");

def fooa(x, y) -> y;
def foox(x) -> fooy(x);
def fooy(x) -> fooz(x);
def fooz(x) -> x;

def foob(x) -> 2;
def fooc(x, y) -> 1;
def food(x, y, z) -> 0;
def fooe(x, y, z, w) -> 8;
