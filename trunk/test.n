def main() -> "fnis":fooa(3, 4);

def fooa(x, y) -> foox(y);
def foox(x) -> fooy(x);
def fooy(x) -> fooz(x);
def fooz(x) -> fuzzing(this);

def fuzzing(v) {
  return "flab".length();
}

def foob(x) -> 2;
def fooc(x, y) -> 1;
def food(x, y, z) -> 0;
def fooe(x, y, z, w) -> 8;

internal class SmallInteger {
  def operator +(that) -> 93;
}

internal class True {

}

internal class String {
  internal def length();
}
