def test_lambda() {
  def f := (fn (x) -> 4);
  assert f() = 4;
}

def test_closure() {
  def a := 6;
  def f := (fn (x) -> a);
  assert f() = 6;
  assert ((fn (x) -> fn (y) -> x + y)(2)(3)) = 5;
}

def main() {
  test_lambda();
  test_closure();
}
