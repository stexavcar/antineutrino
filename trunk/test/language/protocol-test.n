protocol A {
  def x() -> 4;
}

protocol B : A {
  def x() -> 7;
}

def test_inheritance() {
  def a := new A();
  def b := new B();
  assert_equals(4, a.x());
  assert_equals(7, b.x());
}

def main() {
  test_inheritance();
}
