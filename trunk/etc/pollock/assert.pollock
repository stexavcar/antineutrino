def @assert(cause=cnUnknown) ${left:expr} == ${right:expr}
  POLLOCK_ASSERT_EQ(${left}, ${right}, ${cause})

def @assert(cause=cnUnknown) is < ${Type:ident} > \( ${value:expr} \)
  POLLOCK_ASSERT_IS(${Type}, ${value}, ${cause})
