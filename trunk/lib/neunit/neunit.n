internal def fail(value);
internal def print(value);

def assert(value) {
  if (value) return null;
  fail(value);
}

def assert_equals(expected, value) {
  if (expected = value) return null;
  fail("Error: expected <" + expected.to_string() + ">, found <" + value.to_string() + ">");
}
