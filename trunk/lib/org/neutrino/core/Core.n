def print_string(obj is String) => internal "print";

def print(obj) {
  print_string(obj.to_string());
  obj;
}

def fail(message is String) => internal "fail";

def apply(name is String, args is PrimitiveArray) => internal "apply";
