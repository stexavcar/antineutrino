protocol Object;

def are_identical(a, b) -> internal "are_identical";

def (this: Object)=(that) -> are_identical(this, that);

def (this: Object)!=(that) -> not this = that;

def (this: Object).to_string() -> internal "obj.to_string";
